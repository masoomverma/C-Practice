#include <stdio.h>
#include <conio.h>
int main()
{
    clrscr(); 
    int num;
    printf("Enter the number: ");
    scanf("%d",&num);
    if(number % == 0)
    {
        printf("The number is Even.");
    }
    else
    {
        printf("The number is Odd.");
    }
    getch();
    return 0;
}
